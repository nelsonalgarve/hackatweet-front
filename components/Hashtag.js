import { useEffect, useState } from 'react';
import { useSelector } from 'react-redux';

function Hashtag () {


const start = "I will return hastag page"


return(start)
    
}

export default Hashtag;